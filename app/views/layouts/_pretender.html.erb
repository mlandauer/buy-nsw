<% if current_user != true_user %>
  <div class="pretender-alert">
    You (<%= true_user.email %>) are signed in as <%= current_user.email %>
    <%= link_to "Back to admin", stop_impersonating_admin_users_path, method: :post %>
  </div>
<% end %>
